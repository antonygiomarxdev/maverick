---
phase: 02-radio-abstraction-spi
plan: D
status: complete
---

# 02-D Summary

## Delivered

- `docs/hardware-registry.toml` with RAK HAT entries (aarch64 + armv7), schema_version + `boards` rows.
- `docs/lns-config.md`: optional `[radio]` section + link to registry file.

## Deferred

- `scripts/install-linux.sh` archive mention (optional follow-up if release packaging requires it).
